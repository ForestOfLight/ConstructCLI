[package]
name = "construct-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
# Lets `CONSTRUCT_GITHUB_API` redirect the releases client, so the CLI's
# integration tests can serve canned responses. Enabled only through
# construct-cli's [dev-dependencies]; see `install::releases::base`.
test-hooks = []

[dependencies]
thiserror.workspace = true
serde = { workspace = true }
serde_json.workspace = true
toml.workspace = true
directories.workspace = true
tempfile.workspace = true
nbtx.workspace = true
bedrock_level.workspace = true
ureq.workspace = true
zip.workspace = true

[dev-dependencies]
tempfile.workspace = true
tar.workspace = true
flate2.workspace = true
zip.workspace = true
